<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <title>Proyecto Login encriptacion</title>
  <link rel="stylesheet" href="https://bootswatch.com/5/yeti/bootstrap.min.css">

</head>

<body>

  <nav class="navbar navbar-expand-lg navbar-light bg-light" style="background-color: #0E77BD;">
    <div class="container-fluid">
      <a class="navbar-brand" href="#">Login con Encriptacion</a>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav"
        aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarNav">
        <ul class="navbar-nav m-lg-auto">

          <% if (!user) { %>
            <a class="nav-link" href="/signin">Iniciar sesion</a>
            </li>
            <li class="nav-item">
              <a class="nav-link" href="/signup">Registrarse</a>
            </li>
            <% } else { %>
              <a class="nav-link" href="/profile">Perfil</a>
              </li>
              
              <a class="nav-link" href="/QR">QR</a>
              </li>

              <li class="nav-item">
                <a class="nav-link" href="/logout">Salir</a>
              </li>
              <% } %>
        </ul>
      </div>
    </div>
  </nav>


  <div class="container">
    <%if (signinMessage.length> 0) {%>
      <div class="alert alert-danger">
        <p>
          <%= signinMessage %>
        </p>
        <% } %>
      </div>

      <%if (signupMessage.length> 0) {%>
        <div class="alert alert-danger">
          <p>
            <%= signupMessage %>
          </p>
          <% } %>
        </div>

        <%- body %>
  </div>
</body>

</html>